spring.application.name=backend

spring.datasource.url=jdbc:h2:mem:regula
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.sql.init.mode=always
spring.sql.init.platform=h2
spring.sql.init.data-locations=classpath:schema.sql,classpath:data.sql
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true

#H2 database (in-memory)

# JWT configuration
security.jwt.secret=${JWT_SECRET:XiIGnXyfdw7Fw7q86QJe5XGyWNipkARsUshFlrPmTZx}
security.jwt.expiration-ms=${JWT_EXPIRATION:3600000}
security.jwt.invite-expiration-ms=${JWT_INVITE_EXPIRATION:86400000}

# Resend email configuration
email.resend.enabled=${RESEND_ENABLED:true}
email.resend.api-key=${RESEND_API_KEY:re_KTmzPPSh_4LSvPajHVWsQraSkbi7rMyBM}
email.resend.from-email=${RESEND_FROM_EMAIL:Regula <noreply@walmann.party>}
email.resend.base-url=${RESEND_BASE_URL:https://api.resend.com}

# Invite link configuration
email.invite.frontend-base-url=${INVITE_FRONTEND_BASE_URL:http://localhost:5173}
email.invite.path=${INVITE_PATH:/invite/accept}
